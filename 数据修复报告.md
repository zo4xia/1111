# 城厢区换届选举系统 · 数据自洽修复报告

- **修复时间**：2026-09-02（北京时间凌晨；库内 `now()` 为 UTC，健康检查返回 today=2026-09-01）
- **修复范围**：仅数据库数据订正（Neon PostgreSQL）。**未修改** `backend/api.js`、**未修改** `web/src` 任何文件；未重启/占用 8080、3005；未做任何物理删除。
- **基线说明**：上游《城厢区换届选举系统_数据自洽性审计报告.md》形成于 2026-09-01，此后数据已变化（如 elections 2→5、candidates 17→23、materials 6→24，且主线正在产生端到端自测数据 `el-mtiyny8g`）。本报告一切结论以 2026-09-02 实际 SQL 查询为准，旧报告结论仅作线索。
- **修复批次号**：`B20260902`，全部改动前镜像留存于库表 `data_fix_backup`（145 条原始镜像；另含 1 条回滚闭环演练产生的新增行镜像）。

---

## 〇、当前待办事项清单（可直接分派小 agent）

> 以下均为**本次修不了 / 决定不修**的遗留项，按"需要业务输入"或"需要改代码"分类，标注建议承接方。

| # | 待办 | 现状证据（SQL 实测） | 为什么本次没修 | 建议承接 |
|---|------|---------------------|----------------|----------|
| T1 | **补全选民名册**：el-11 voters 仅 23 人，election_results.er_eligible_voters=1286，缺 1263；el-15 voters 仅 15 人且无汇总数 | voters 分组：el-11=23、el-15=15 | 真实名册属业务数据，不能臆造 | 数据小 agent + 业务导入 |
| T2 | **el-15 候选人终态与结果补全**：修复后仍有 6 人"待第3轮"、2 人"待第4轮考察"；需业务给审核结果后录入，并据此补全副主任/委员的 election_results 及应有选民/实投/有效/无效票汇总（本次仅补了有证据的主任 1 条） | candidates el-15 状态分布：当选1/待第3轮6/待第4轮2/联审不通过1/预选未入围1 | 四轮联审/考察结果无数据来源，不能替业务判定通过与否 | 业务确认 → 经办小 agent 走 `/api/candidates/:id/round` 录入 |
| T3 | **el-11 新旧班子交接**：6 条结果 er_handover_status 全部"未交接"（选举日 2026-07-16，已 D+48）；交接实际完成后，把结果改"已交接"、把 D+1~+10 阶段由"进行中"改"已完成" | election_results el-11：6 行"未交接"；阶段 D+1~+10 现为"进行中" | 交接是否完成是事实问题，系统里无证据 | 业务跟进 → 数据小 agent |
| T4 | **公告正式文号**：el-11/el-15 各 3 条已发布公告 ann_code 为"预选办法/预选结果/补充公告"（共 6 条），需业务确定正式文号后统一为"第N号"；el-14（2021 历史届）4 条结果 er_result_ann_code 为空，需查纸质档案 | announcements 非规范编号 6 条（均有正文、已 published）；el-14 空编号 4 条 | 已发布公文的编号不能由技术侧编造；el-14 标注"历史届·只读" | 业务/文书 → 数据小 agent |
| T5 | **组织主数据**：123 个组织 org_phone、org_person **全部**为 null；其中 84 个"XX占位N"已在 org_note 打标（未删、status 保持 active）。需补真实联系人/电话，并由业务确认占位组织的去留（删除属物理删除，本次红线禁止，需另行授权后处理） | organizations：total 123 / 占位 84 / phone 空 123 / person 空 123 | 联系信息无来源；占位清理涉及删除 | 主数据小 agent + 业务确认 |
| T6 | **系统超管账号**：acc_phone=`130****0000`（脱敏、含 * 无法登录）、密码 hint=`*`，acc_note 自述"引导账号不参与登录演示"。本次仅把 roles 补为 platform_admin；若要启用需业务提供真实 11 位手机号 | accounts 单行 | 无法恢复真实手机号；且其余 11 个账号均为明文手机号（登录依赖，不能反向脱敏） | 业务/管理员 |
| T7 | **张桂香材料未入候选人池**：el-15 材料 `c96e6e7f…` 已 approved，但 candidates 中无同手机号（15800001044）人员，mat_candidate_id 保持 null；需核实是补建候选人还是材料作废 | materials 左连接 candidates 后唯一未匹配行 | 无候选人记录可关联，不能凭空建人 | 经办小 agent 核实 |
| T8 | **滞留提案**：`第15届选举委员会成立方案`（id 9a3a7868…）prop_status='submitted'（应用枚举只有 pending/approved/rejected），2026-06-22 提交至今未处理，需业务审批或驳回。另：prop_creator_id/prop_reviewer_id 存手机号是 api.js 现行写法（创建即写 u.phone），**属设计而非脏数据**，若要改为账号 UUID 必须代码+数据一起改，超出本次范围 | proposals：5 行，creator 全为手机号、reviewer 4/5 为手机号 | 审批是业务动作；改 ID 体系牵涉应用代码 | 业务审批；ID 体系交后端小 agent |
| T9 | **P0-1 前端 mock 两张皮**：前端硬编码 mock 数据、未全面对接真实 API，属代码层问题 | 见原审计报告 P0-1 | 本次任务红线：禁止改 web/src | 前端小 agent（独立任务） |
| T10 | **岗位字段是否改 UUID 外键（设计决策）**：candidates.cand_position_id / materials.mat_position_id 存中文"主任/副主任/委员"，但 api.js 自身新增候选人/材料时写的就是中文，且全系统无 candidates→positions 的 JOIN，中文值与 positions.pos_type 100% 对得上。本次按"设计如此"保留；若要改为 positions.id 外键，需代码与数据联动迁移 | 23 名候选人的中文岗位 100% 能在同届 positions.pos_type 匹配 | 改成 UUID 会让现行 api.js 直接显示 UUID、反而制造故障 | 架构决策后交后端+数据联合处理 |
| T11 | **materials 附件文件（matFiles）**：附件实体在磁盘 `backend/uploads/`，不属数据库字段，本次未核对文件缺失情况 | — | 非数据层问题 | 运维/后端小 agent |
| T12 | **自测数据治理**：主线端到端自测选举 `el-mtiyny8g`（标题含"端到端自测"）及其 17 条 draft 公告、岗位、阶段正在库中，本次严格未触碰；测试结束后是否清理由主线决定 | elections 中 el-mtiyny8g in_progress、选举日 2026-12-30 | 红线：禁止删除测试期新建记录 | 主线/测试小 agent |

---

## 一、合法角色枚举查证（P0-3 的判定依据，只读代码）

1. **`backend/api.js`（只读）**：
   - 登录仅对 `accounts.roles === 'platform_admin'` 特殊处理（→ role=admin、crossOrg=true、roleKeys=['platform_admin']）；小程序注册写入 `roles='voter'`；其他值一律 role=user。
   - 业务权限来自 **account_roles.role_key**，`STAFF_ROLES = ['operator','sub_admin','editor','reviewer','platform_admin']`（api.js 第 77-78 行）。
2. **库内 `roles` 字典表（6 行，实测）**：`platform_admin`（平台超管）、`sub_admin`（村/社区子管理）、`operator`（经办/书记）、`editor`（运营编辑）、`reviewer`（审核）、`voters`（选民）。
3. **`web/src/utils/roleStore.ts`（只读）**：super_admin/sub_admin/editor/reviewer/candidate 是**前端本地演示 store**，不落库。
4. **结论**：代码与字典中**不存在** org_admin、filer 等角色码；`NO.00x` 不是任何合法角色。每个账号的目标角色以其 **account_roles 已授权的 role_key**（11 行，全部 active）为直接证据，无需主观猜测职务。

---

## 二、修复明细（每项：现状证据 → 动作 → 前后对照 → 回读验证）

### A. P0-3 accounts.roles 非法值（11 行）

**现状证据（修复前实测）**：12 个账号中 11 个 roles 为 `NO.001~NO.006`；account_roles 中 10 个业务账号各有恰好 1 条 active 授权（张主任已是 platform_admin）。

**修复动作**：`roles ~ '^NO\.'` 且恰好 1 条 active 授权时，roles 同步为该 role_key；"系统超管"无授权行，按其名称置为 platform_admin。幂等守卫：已一致则不动。

**前后对照（11 行）**：

| 账号 | 手机号 | 归属 | 修复前 | 映射依据（account_roles/职务备注） | 修复后 |
|---|---|---|---|---|---|
| 系统超管 | 130****0000 | boss | NO.001 | 名称即系统超管（无 account_roles 行） | platform_admin |
| 李经办 | 13800000002 | s-jiankou | NO.003 | role_key=operator（经办/书记） | operator |
| 王审核 | 13800000003 | s-jiankou | NO.005 | role_key=reviewer（审核员） | reviewer |
| 赵运营 | 13800000004 | s-jiankou | NO.004 | role_key=editor（运营编辑） | editor |
| 林建国 | 13800000005 | s-jiankou | NO.002 | role_key=sub_admin（选委会主任） | sub_admin |
| 黄志明 | 13800000006 | s-jiankou | NO.006 | role_key=voters（H5 选民/候选人） | voters |
| 吴春霖 | 13800000011 | v-yanshou | NO.002 | role_key=sub_admin（选委会主任） | sub_admin |
| 郑文彬 | 13800000012 | v-yanshou | NO.003 | role_key=operator（经办） | operator |
| 陈玉华 | 13800000013 | v-yanshou | NO.005 | role_key=reviewer（审核员） | reviewer |
| 林巧云 | 13800000014 | v-yanshou | NO.004 | role_key=editor（运营编辑） | editor |
| 徐永福 | 13800000015 | v-yanshou | NO.006 | role_key=voters（H5 选民/当选主任） | voters |

张主任（13800000001）已是 platform_admin，未动。**手机号脱敏不一致问题**：库内登录账号统一为明文手机号（登录按手机号匹配，不能脱敏）；仅"系统超管"为脱敏引导账号，保持原样并列入 T6。

**回读验证（05_verify.js）**：✅ `roles ~ '^NO\.'` 残留 0；✅ 每个账号 roles 与其 account_roles.role_key 不一致数 0。
**API 冒烟（07_api_smoke.js，只读访问运行中的 8080）**：张主任→admin/[platform_admin]；王审核→reviewer；吴春霖→sub_admin；黄志明→voters，均登录成功。

---

### B. P0-2 el-11 / el-15 选举状态与备注（2 行）

**现状证据**：修复前 el-11（选举日北京 2026-07-16）、el-15（北京 2026-07-30）均为 in_progress，距今分别 D+48、D+34；el-11 备注"D+5收尾中：备案完成、交接未开始"严重滞后；el-15 备注为设计说明"D日锚点=全系统唯一日期输入"。el-10/el-14 为 archived 历史届，不动。

**前后对照**：

| el_id | el_status 前→后 | el_note 前→后 |
|---|---|---|
| el-11 | in_progress → **finished** | "D+5收尾中：备案完成、交接未开始" → "已于2026-07-16完成投票选举，选举结果已备案（第17号公告）；新旧班子交接尚未完成，待跟进（2026-09-02数据订正）" |
| el-15 | in_progress → **finished** | "D日锚点=全系统唯一日期输入" → "已于2026-07-30完成投票选举；结果仅补录主任岗位（黄志明28票），其余岗位结果及选民汇总数据待业务补录（2026-09-02数据订正）" |

**关于"1 天偏差"的实测纠正（重要）**：elections.el_election_date、election_stages.*_date、election_results.er_election_date 列类型**都是 `date`**（非 timestamptz）。node-pg 会把 date 解析成 UTC 零点的 JS Date，ISO 显示为"前一日 16:00Z"，旧报告把这个**显示层假象**当成了存储偏差。按 `AT TIME ZONE 'Asia/Shanghai'` 实测：**el-11 的选举日、D0 阶段、结果日三者均为 2026-07-16，本来就一致，未改任何日期**。真正错位的是 el-15（见 C）。

**回读验证**：✅ el-11/el-15 均 finished；✅ 自测选举 el-mtiyny8g 仍 in_progress 未受影响。

---

### C. el-15 全部 16 个阶段日期早 1 天（16 行）

**现状证据**：以选举日 D=2026-07-30 按 stage_templates 偏移逐阶段比对，el-15 的 16 个阶段**全部** start_diff=-1（D0 阶段为 07-29，应为 07-30；D-35 为 06-23，应为 06-24……）；对照组 el-11 16 个阶段 diff 全 0。

**修复动作**：按 `stage_start_date = el_election_date + st_day_offset` 对齐，end_date 平移相同增量（保留各阶段自身宽度）。示例前后（均为 date 原值）：

| stage | 前 start~end | 后 start~end |
|---|---|---|
| D-35 前期筹备 | 2026-06-24（注：旧值 06-23） | 2026-06-24 |
| D0 正式投票 | 2026-07-29 | **2026-07-30** |
| D+1~+10 备案交接 | 2026-07-29 ~ 2026-08-07 | **2026-07-30 ~ 2026-08-08** |

**回读验证**：✅ el-15 16/16 对齐；✅ el-11 16/16 仍对齐（未被误伤）。阶段状态原本就全是"已完成"，未改。

---

### D. el-11 收尾阶段状态词不合法（1 行）

**现状证据**：el-11 的 D+1~+10 阶段 stage_status="办理中"，而日程引擎只产生"未开始/进行中/已完成"，前端 Home 也只统计这三个值，"办理中"会落到所有计数之外。该阶段对应"结果备案、交接"，而 election_results 恰显示交接未完成。
**动作**："办理中"→"**进行中**"（如实表达交接逾期未完成，仪表盘会出现到期提醒，比隐藏更正确；待 T3 业务完成交接后再改"已完成"）。
**回读验证**：✅ 该阶段=进行中。

---

### E. P1-2 候选人轮次列/状态自洽（4 行；另 4 行"落选"明确不动）

**判定基准**：api.js `deriveStatus()`（轮次结果唯一派生 cand_status 的规则）。修复前用脚本 04 全量扫描 23 人。

**修复 4 人（前后对照）**：

| 选举 | 姓名 | 修复前（r1/r2/r3/r4/status） | 修复后 | 理由 |
|---|---|---|---|---|
| el-11 | 郑阿土 | 通过/通过/**r3=预选未入围（非法轮次值）**/∅/预选未入围 | 通过/**不通过**/**NULL**/∅/预选未入围 | "预选未入围"按派生规则应由 r2=不通过 表达，r3 被误写了状态值 |
| el-11 | 许金坤 | 同上 | 同上 | 同上 |
| el-15 | 林强 | 通过/不通过/∅/∅/**初审退出** | 通过/不通过/∅/∅/**预选未入围** | r2=不通过 的派生结果就是"预选未入围"，"初审退出"对应 r1=不通过，自相矛盾 |
| el-15 | 黄志明 | 通过/通过/**待审**/**∅**/当选/28票 | 通过/通过/**通过**/**通过**/当选/28票（cand_note 追加订正说明） | 已录"当选+28票"，按派生规则当选必须 R3/R4 均通过，补齐使其自洽 |

**明确不动的 4 人**：el-11 李国雄、王国祥、许丽英、翁志强——四轮全通过且有得票、状态"落选"。这是**多席位选举的真实结果**（主任1/副主任1/委员4，得票少者落选），其"当选"名单与 election_results 的 6 名胜者完全吻合；deriveStatus 对多席位简化（votes>0 即当选），不能用它覆盖业务真值。
**其余 8 名 el-15 候选人**（6 待第3轮、2 待第4轮）无业务结果，保持原样 → T2。

**回读验证**：✅ 4 人目标值全部落位（05_verify E 节）。

---

### F. el-15 岗位状态（3 行）

**现状**：el-15 三个 positions（主任/副主任/委员）pos_status='open'，而选举已结束；同库已结束的 el-11 对应岗位均为 'closed'。
**动作**：el-15 的 open → **closed**（幂等：只改 open）。**回读**：✅ 3 行全部 closed。

---

### G. P2-2 materials.mat_candidate_id 存姓名 → 候选人 UUID（23 行）

**现状证据**：24 份材料该字段全部是中文姓名（0 个 UUID、0 个能与 candidates.id 关联）；而 api.js 材料审核通过后回写的本应是 candidates.id（送审邮件也按 id JOIN）。
**修复动作**：与应用同款去重键"同选举 + mat_submitter_phone = cand_phone"且唯一匹配时，回写候选人 UUID；已是 UUID 的不动。
**结果**：el-11 12/12、el-15 11/11 共 **23 行**完成关联；唯一未匹配的是 el-15 张桂香（15800001044，approved 材料但候选人池无人），保持 null → T7。
**回读验证**：✅ UUID 数与 JOIN 命中数 12+11；✅ 张桂香仍为 null（未臆造）。

---

### H. P2-3 elections.el_proposal_id 回填（2 行）

**现状证据**：4 个 elections 的 el_proposal_id 全 null；proposals 中 el-11、el-15 各有恰好 1 个 approved 主提案。
**动作**：仅当该选举 approved 提案恰好 1 个且字段为 null 时回填：el-11→`9ab2389f…`（第11届延寿村委会换届方案）、el-15→`8e5935b0…`（第15届涧口社区居委会换届方案）。
**不处理**：prop_creator_id/reviewer_id 存手机号是 api.js 自身行为（写入 u.phone），属设计 → T8；submitted 滞留提案 → T8。
**回读验证**：✅ 两条关联均已建立。

---

### I. P1-3 el-15 补录选举结果（新增 1 行）

**现状证据**：el-15 在 election_results 中 0 行；candidates 中唯一有得票的是主任黄志明（28 票、当选）。
**动作**：幂等插入 1 行（不存在才插）：org=s-jiankou/涧口居委会、er_election_date=2026-07-30、主任/黄志明/28 票；er_note 注明来源与"汇总数据待补"；应有选民/实投/有效/无效票等**无证据的汇总列一律留空，不臆造**。
**回读验证**：✅ el-15 结果 1 行且字段正确。其余岗位结果 → T2。

---

### J. P1-1 占位组织"打标不删除"（84 行）

**现状证据**：organizations 共 123 个，名称含"占位"的 84 个；org_phone/org_person 全表 123 个皆 null。
**动作**：遵守"只标记、不删除、不改 status（避免影响登录页归属地下拉）"，仅对名称含"占位"者写入 org_note 标记："占位组织（2026-09-02数据订正标记：待业务核实后补全或清理，本次不删除）"。
**回读验证**：✅ 84/84 打标、84/84 status 仍为 active、0 删除。补联系人与后续清理 → T5。

---

## 三、逐项核查后"不改"的问题（含旧报告失实项）

| 线索 | 实测结论 | 处置 |
|---|---|---|
| P0-2 el-11 选举日与 stages/results 差 1 天 | **失实**：列类型为 date，按北京时间三者均 2026-07-16，完全一致；偏差是 UTC 解析显示假象 | 不改，已在 B 节举证 |
| P2-4 阶段用 UTC 时间存储导致偏移 | **失实**：阶段列是 `date`，不存在 UTC 时间戳；16:00Z 是驱动显示 | 不改 |
| P2-4 el-11 缺 D+1~D+10 阶段 | **已不存在**：el-11/el-15 现均为完整 16 阶段（共 32 行种子 + 自测选举 16 行） | 不改 |
| P1-2 candPositionId 存中文 | api.js 自身就写中文岗位名、无任何 candidates→positions JOIN，且中文值与 positions.pos_type 100% 匹配 | 保留，T10 |
| P1-3 el-11 结果公告编号为 null | **失实**：el-11 六条均为"第17号"；为 null 的是 2021 历史届 el-14（4 条，只读） | el-14 → T4 |
| P1-3 el-11 交接"未交接" | 与 el-11 备注一致，业务尚未交接，不能替它改"已交接" | T3 |
| P1-4 选民名册缺口 | 真实业务数据缺失，不能编造 | T1 |
| P2-1 公告编号不统一 | 6 条描述性编号均已发布且有正文，正式文号需业务给 | T4；自测选举 draft 公告不碰（T12） |
| P1-1 占位组织 | 打标 84 个，不删除 | T5 |
| el-15 其余候选人/结果、张桂香、submitted 提案、超管手机号、材料附件 | 均需业务输入或代码改造 | T2/T6/T7/T8/T11 |

---

## 四、脚本与可复现性（backend/scripts/）

| 文件 | 作用 | 性质 |
|---|---|---|
| `_db.js` | 读取 neon.env 连接串、构造 pg Pool（ssl 参数同 api.js） | 公共 |
| `01_inspect.js` / `02_inspect_compact.js` / `03_inspect_schema.js` | 全量只读排查（表/行/约束/对齐校验） | 只读 |
| `04_cand_consistency.js` / `04b_precheck.js` | 候选人状态机扫描、修复前安全预检 | 只读 |
| **`10_fix_data.js`** | **幂等修复主脚本**（事务、先备份后改、守卫条件；`$env:DRY=1` 可演练） | 修复 |
| **`99_rollback.js`** | **整批回滚**（op=I 删除新增、op=U 按镜像整行还原；加 `purge` 参数同时清备份） | 回滚 |
| `05_verify.js` / `06_spot.js` / `07_api_smoke.js` / `08_legacy.js` | 回读硬校验、快速抽查、8080 登录冒烟、遗留项数字汇总 | 只读 |
| `*_out.txt` | 各脚本实际运行输出（证据留档） | 证据 |

**操作记录**：
1. DRY 演练 → 预计改动 A11/B2/C16/D1/E4/F3/G23/H2/I1/J84；
2. 正式 APPLY：一次 COMMIT，**执行无报错**，计数与演练一致；
3. **幂等性**：紧接着第二次执行，10 个节全部"备份 0 / 改动 0"；
4. **回滚闭环实测**：执行 `99_rollback.js` 后抽查，11 个 NO. 角色、两个 in_progress、el-15 零结果、材料零 UUID、84 个未打标、"办理中"、林强"初审退出"全部恢复修复前原貌；随后再次执行 `10_fix_data.js` 重新修复并通过 `05_verify.js` 全部硬性校验（✅）。
5. 备份表 `data_fix_backup` 留存原始镜像：accounts 11、election_stages 17、elections 2、candidates 4、positions 3、materials 23、election_results 新增镜像、organizations 84。

**回滚方法**：`cd backend; node scripts/99_rollback.js`（幂等，可重复执行；回滚后如需再修复，重跑 `node scripts/10_fix_data.js` 即可）。

---

## 五、修复总览（数字台账）

| 节 | 对象 | 改动类型 | 行数 | 回读 |
|---|---|---|---|---|
| A | accounts.roles | UPDATE | 11 | ✅ |
| B | elections（el-11/el-15 状态+备注） | UPDATE | 2 | ✅ |
| C | el-15 election_stages 日期对齐 | UPDATE | 16 | ✅ |
| D | el-11 收尾阶段状态 | UPDATE | 1 | ✅ |
| E | candidates 轮次/状态 | UPDATE | 4 | ✅ |
| F | el-15 positions 关闭 | UPDATE | 3 | ✅ |
| G | materials 候选人关联 | UPDATE | 23 | ✅ |
| H | elections 提案关联 | UPDATE | 2 | ✅ |
| I | el-15 主任结果 | INSERT | 1 | ✅ |
| J | 占位组织打标 | UPDATE | 84 | ✅ |
| 合计 | — | 147 处订正、0 删除 | 147 | 全部 ✅ |

未触碰：web/src、api.js、8080/3005 进程、el-10/el-14 历史届、主线自测数据 el-mtiyny8g、所有业务真值（落选/未交接/真实得票）。

*报告完毕。*
