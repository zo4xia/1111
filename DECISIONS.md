# 决策记录 DECISIONS.md — 城厢区村居换届选举系统

> 本文件是系统的「架构宪法」，任何页面/接口改动不得违背。新决策追加在最前。

---

## D-000｜骨架钉子：系统世界观与不变量（2026-09-02 甲方只讲一次 · 最高约束，凌驾所有页面实现）

> 这是整套系统的「骨架」。任何代码、sub、旧包复用都必须先符合它，不符合的旧实现一律不沿用。

1. **系统性质**：面向**政务组织用户的基层换届选举「内部管理系统」**。**不存在「选民」这个角色/模块/端**（不是给选民投票用的 C 端）；旧代码/旧包里凡出现「选民端、选民注册、选民投票、面向村民的前台」一律不继承，只保留"选民登记/名册"这类**内部经办数据**概念。
2. **两类组织，一套流程**：组织分「村委会（village，村）」与「社区居委会（community，社区）」，共 **130+ 个**。两者 SOP **大体相同**，差异**只在选举细节与岗位设置**——用 `org_type` + 模板/岗位配置区分，**禁止复制两套流程代码**。
3. **每组织独立选举日，D 为锚**：每个村/社区各有自己的选举日期；**D = 选举日，完整业务 SOP = 一条按 D 倒排的 pipeline**。
4. **复用铁律（一个通，全部通）**：**一个村跑通，所有村复用，只是数据内容不同；社区同理。** 一套代码 + 数据驱动，严禁为单个组织写定制逻辑；新增组织 = 新增数据，不是新增功能。
5. **防串台三要素（多租户隔离）**：**归属地(org) + 账号 + 密码，三者缺一不可**，登录与每次读写都据此锁定唯一组织上下文；所有查询/写入默认带 org 隔离，A 村绝看不到/改不到 B 村数据（仅 platform_admin/crossOrg 超管可跨组织，且必须显式）。
6. **旧包复用边界**：sub 从旧包**只捞 UI 风格与可复用代码片段**；先过 D-000/D-001 筛子——不含"选民 C 端"、不造第二个编辑入口、不搬 mock 假数据，能用则抽成公共模块，不能用则弃。

---

## D-001｜系统本质是一条 Pipeline，只保留两个可编辑入口（2026-09-02 甲方拍板 · 最高优先级）可以这样记住：记录下项目记忆，能够编辑的集中在 提案审批  活动列表  两个地方，其他的：材料审核和候选人管理属于到了时间，甲方人工线下配合审核，回填结果，更新候选人情况的。所以其他的页面只是模板和查看，其实没有什么实质作用。这样可以理解了没有？活动列表里面，当届的时间轴有小编工作台，我们的预览公文，和公告管理里面的公告记录点击编辑的地方，其实功能重复，为什么不可以调用模块，这样不会造成两个地方都要编辑，两个真相让系统觉得摸不着头脑，点击预览公文 个人觉得公告管理里面的像是正确的有做每个不同公告的内容是否有每个公文有下载链接，（因为有的时候一天发3个公告，可是前端点击了切换公告选项，下面的下载链接什么的我感觉是死的。整个系统就是这样两个真相才会凌乱不堪）1、我喜欢公告管理里面每个公告每个公告都很清晰的  2、但是小编工作台的编辑内容下面的发布方式模块和公文的切换做的很好。  思考怎么结合起来，编辑、预览、放在小编工作台   公告通知里面，公告记录，就变成只要记录发没发， 这样，思考，然后系统不允许两个地方设置，违反我的唯一性原理。

### 一句话定义
整套系统 = **一条换届流水线**：提案审批通过 → 锚定 D0 → 沿 16 阶段时间轴自动推进，每个阶段该做的事在「活动详情·小编工作台」就地完成。

### 唯一允许「编辑/写数据」的地方（只有两个）
| 编辑入口 | 负责写什么 |
|---|---|
| **① 提案审批 Proposals** | 提案内容、D 日、岗位行、提案/岗位报名附件；审批通过动作（触发整条 pipeline 生成） |
| **② 活动详情·小编工作台 ActivityDetail** | 按阶段编辑/预览/发布公文、发布方式（立即/定时）、附件下载、材料提交开关、日程提醒；到点事项的处理入口 |

### 其余页面的定位（只读 / 模板 / 结果回填，不得再做内容编辑）
| 页面 | 定位 | 允许的动作 |
|---|---|---|
| 公告记录 Announcements | **只读台账**：只记录每份公告「发没发/何时发」 | 查看、跳转到小编工作台编辑、发布状态切换；**不允许在本页编辑正文** |
| 快捷模板 QuickTemplates | **只读公文模板库**，供小编一键引用/复制 | 复制、跳转工作台 |
| 岗位管理 Positions | 提案自动生成后的**查看** + 岗位附件补传 | 查看、上传/下载岗位附件（写附件，不写岗位结构） |
| 材料审核 Materials | **到点后甲方线下审核的结果回填** | 代传附件、审核通过/驳回（结果记录，非内容编辑） |
| 候选人管理 Candidates | **到点后甲方线下配合的结果回填** | 四轮公示结果录入、票数/状态更新（结果记录） |
| 历史归档 Archives | 只读归档 | 查看/下载 |
| 首页工作台 Home | 只读看板（真实数据，禁止 mock 兜底造假） | 查看、跳转 |

### 单一真相源原则（唯一性原理 · 不可违反）
1. 同一份数据**有且仅有一个写入口**；其他页面只能读、或带参数跳到写入口，禁止第二处编辑表单。
2. 编辑态字段必须**真实落库**，不允许只存在前端内存（刷新即丢 = 未完成）。
3. 同一数据在任何页面的**预览长相必须一致**（公文统一用 `components/NoticeDoc`，禁止再写第二套 `<pre>` 预览）。
4. 凡是「点了只弹提示、不产生真实效果」的按钮 = 缺陷（要么接真，要么跳到对应回填页，不允许 `MessagePlugin.info` 占位）。

### 由本决策直接推导出的整改方向（详见 TodoList）
- 公告记录**去编辑化**：删掉本页编辑弹窗，改为只读列表 + 「去小编工作台处理」跳转；发布动作可保留。
- 小编工作台**成为公文唯一编辑器**：补齐落款/成文日期/多附件（真实上传）/材料开关/定时/提醒，预览统一换成 NoticeDoc。
- 后端 announcements 表**补列**：落款、成文日期、附件 JSON、材料开关、发布方式/定时时间、提醒设置，PUT 全量持久化。
- 小编工作台的「查看材料 / 更新候选人」改为**跳转到材料审核 / 候选人管理的对应回填位**，不在工作台内造假按钮。

---

## D-002｜审批通过在一个 DB 事务内联动生成（2026-09-01）
提案 approve → 事务内原子抢占（`WHERE prop_status='pending' RETURNING`，防连点重复）→ 锚定 elections.D 日 → 覆盖式重建 16 阶段日程（统一用 `shiftDate` 纯日期，禁用 `toISOString` 防 UTC 偏移一天）→ 覆盖式重建岗位 → 幂等补 18 条 draft 公告。已验证：D0=D 日、16 阶段、岗位数正确、18 草稿、重复审批被 400 拦截。

## D-003｜前端一律接真实 API，禁止内存 mock 两张皮（2026-09-01）
提案/审批/岗位/公告/上传全部走后端；上传用 multer 落盘 `backend/uploads/<域>/<id>/`，静态服务 `/api/files/`。TDesign Upload 无 action 时用本地占位 requestMethod 仅为消除组件报错，真实文件必须在提交/选定时调真实上传接口，且需注释说明，命名不得让人误判为"已上传"。

## D-004｜选举业务页统一「两级结构」，物理归属不混届（2026-08-31 钉子）
1. 所有选举业务页（材料/候选人/公告/岗位）一律两级：**第一级=历届活动列表**（复用 `components/ElectionSessionList`），**第二级=该届明细**（顶部 `SessionDetailBar`：←返回+届次+活动名+方式+Dday）。禁止跨届扁平大表、禁止右上角下拉切届。
2. 任何明细必须带 `election_id/activityId` 归属某一届，列表查询必须支持并默认按届过滤（参照 positions 补 electionId 的修法），**绝不跨届混表**。
3. 复用 `sessionNo`（提届次）、`calcActivityStatus`（纯本地日期算状态，禁 UTC），禁止各页重复造。

## D-005｜小程序/参选人端：提案通过即定型 + 按日程分时段解锁（2026-08-31 钉子）
1. **提案审批通过那一刻，小程序展现内容全部确定**（Dday、岗位/选举方式、1-17 号公告模板、日程），小程序**只读活动数据、自己不造数据**（数据源 = 提案通过后生成的活动，岗位/选举方式与后台「岗位管理明细」同源）。
2. **分时段可见**：未到发布日期的未来公告/日程对参选人**隐藏或置灰**，仅管理侧可见；到点由 election_stages 驱动解锁，节奏=pipeline。
3. 参选人端本质只有三件事：**展示 + 收集材料 + 看最近通知**；无任何编辑/发布按钮；材料窗口由公告「开放材料提交」开关按日程开闭。

## D-006｜角色权限矩阵（归属地漏斗：归属地→角色→权限→账号）
| 角色 | 能编辑 | 能审批 | 其他 |
|---|---|---|---|
| 超管 platform_admin | ✅ 全部归属地 | ✅ | 唯一可创建/管理角色，可跨组织(crossOrg) |
| 子管理 sub_admin | ✅ 本组织 | ✅ | 可增删本组织人员，**不可建角色** |
| 经办编辑 operator/editor | ✅ 可编辑/新建 | ❌ 不可审批 | — |
| 审核员 reviewer | ❌ 不可编辑 | ✅ 可审批 | — |
| 参选人 candidate | ❌ | ❌ | 仅下载资料/到点提交材料/看公示 |

## D-007｜voters 冻结、差异落位、废弃清单（重要，防止做反方向）
1. **`voters` 表/「选民作为登录角色」是错误残留：冻结、不展示、无登录入口、不补数据**；有资格参选的人一律走 `candidates`（自荐/组织推荐）。→ 数据报告中"补选民名册到上千人"的待办**撤销**，方向相反。
   - ⚠️ **术语精确边界（不可机械删词）**：被废的是「选民=系统用户/角色/投票 C 端」；而 16 阶段 SOP 里的**法定阶段名「选民登记 / 公示选民名单 / 受理选民申诉」是经办人员登记辖区有选举权居民名册的内部业务动作，属于内部数据，必须保留**（stage_templates/election_stages 里这些名字不动）。判断准则：出现"选民"时，凡指**可登录/可投票的人**→删；凡指**经办登记的名册或法定阶段**→留。
2. **村/社区差异只允许落在数据配置**：`stage_templates.org_type`（模板差异）、`positions.pos_type/pos_name`（岗位差异）、公告称谓自动适配；**禁止为村/社区各做一套页面或流程**。
3. **下游不得新建结构**：岗位/日程/公告只在提案通过时生成，下游页面只能展示或微调补传，不能新建岗位/阶段/公告结构。
4. **明确废弃、不得复活**：飞书多维表格方案；g5/g6 等他人旧代码（含"选民"模型、硬编码外部平台，**只可抄 UI 风格/片段，不可用其结构**）；选民登记/投票类 C 端功能。
5. **时间轴每个阶段必须给经办留操作位**（不能只展示）：发布公告/查看材料/更新候选人等按阶段类型出现；这些按钮要接真或跳到对应回填页，不允许 `info` 假占位（见 T4）。

## D-008｜工程边界：三个工作包与附件自生成（2026-09-02 甲方确认）
1. **唯一工作对象 = 项目内三个子工程**：`backend`（Node+Express+Neon PG，唯一主文件 api.js）、`web`（React+Vite+TDesign 管理后台）、`miniprogram`（微信小程序，参选人/经办端，已对齐「清理版」）。除此之外磁盘上的历史 zip/tar 都是参考，不直接改、不整体搬。
2. **旧包复用只取片段**：从旧包只捞 UI 风格与可复用代码片段，且必须先过 D-000/D-007 筛子（无选民 C 端、不造第二编辑入口、不搬 mock），抽成公共模块复用，禁止代码膨胀。
3. **附件一律自动生成**：测试/演示所需的上传附件（报名表、公文附件、图片、docx/pdf 样例）由我们用脚本/工具自行生成合规占位文件，不等待外部提供；生成的测试数据与文件在交付前可一键清理。
