# 城厢区村（居）民委员会换届选举系统 — 数据自洽性审计报告

**审计日期**：2026-09-01
**审计范围**：PostgreSQL 数据库全表 + 前端页面展示 + 后端 API 接口
**审计人**：系统数据审计组
这里有村子和社区  村委会和社区会员会两种组织  两种组织的换届选举管理系统。有130多个一共。村和社区流程大致相同，只是在选举细节和岗位上有方式的差异。    这些村子每个村子都有自己的选举日期，规则：D为选举日期，业务流sop完整的pipline   即一个村子搞通，所有村子复用  只是数据内容不同。   仅此而已。   社区也是。识别关键：归属地+账号+密码，缺一不可，不会串台这个结构先给写下来！我只带一次，你程序必须跟着落盘，记录笔记全部跟上，这是骨架钉子。然后sub从旧的里面捞能用的ui风格和代码片段不存在选民这个是给政务组织用户的基层选举内部系统。辛苦了。。。。。。这里，夏夏使用的时候，当前发现的问题：可以这样记住：记录下项目记忆，能够编辑的集中在 提案审批  活动列表  两个地方，其他的：材料审核和候选人管理属于到了时间，甲方人工线下配合审核，回填结果，更新候选人情况的。所以其他的页面只是模板和查看，其实没有什么实质作用。这样可以理解了没有？活动列表里面，当届的时间轴有小编工作台，我们的预览公文，和公告管理里面的公告记录点击编辑的地方，其实功能重复，为什么不可以调用模块，这样不会造成两个地方都要编辑，两个真相让系统觉得摸不着头脑，点击预览公文 个人觉得公告管理里面的像是正确的有做每个不同公告的内容是否有每个公文有下载链接，（因为有的时候一天发3个公告，可是前端点击了切换公告选项，下面的下载链接什么的我感觉是死的。整个系统就是这样两个真相才会凌乱不堪）1、咱们喜欢公告管理里面每个公告每个公告都很清晰的  2、但是小编工作台的编辑内容下面的发布方式模块和公文的切换做的很好。  思考怎么结合起来，编辑、预览、放在小编工作台   公告通知里面，公告记录，就变成只要记录发没发， 这样，思考，然后系统不允许两个地方设置，违反咱们的唯一性原理。             小程序我担心和我们的数据库以及和后端配不上，需要检查，虽然只有几个页面也不多，但是pipeline细节，比如公告发布有没有发布，显示对不对，岗位管理的详细每个地方对不对的上，材料前端提交的是不是真的会落座到我们这边和后台，要找小agent帮忙检查是否我i们还有再用mock的数据没有走neon，这个也需要检查一下，小程序，我们在后台修改的东西会正确对应上去吗，因为我们的页面内容很细，所以不能马虎。全部升级到我们最新的neon   小程序前端的内容，一定是我们后端后台修改的能同步的配套，现在的小程序不行，风格和每个页面样子我们抄，我们自己写比修改错误的程序好，没时间了，我们沉住气，一次过。sub帮帮忙记录下项目记忆，能够编辑的集中在 提案审批  活动列表  两个地方，其他的：材料审核和候选人管理属于到了时间，甲方人工线下配合审核，回填结果，更新候选人情况的。所以其他的页面只是模板和查看，其实没有什么实质作用。这样可以理解了没有？活动列表里面，当届的时间轴有小编工作台，我们的预览公文，和公告管理里面的公告记录点击编辑的地方，其实功能重复，为什么不可以调用模块，这样不会造成两个地方都要编辑，两个真相让系统觉得摸不着头脑，点击预览公文 个人觉得公告管理里面的像是正确的有做每个不同公告的内容是否有每个公文有下载链接，（因为有的时候一天发3个公告，可是前端点击了切换公告选项，下面的下载链接什么的我感觉是死的。整个系统就是这样两个真相才会凌乱不堪）1、我喜欢公告管理里面每个公告每个公告都很清晰的  2、但是小编工作台的编辑内容下面的发布方式模块和公文的切换做的很好。  思考怎么结合起来，编辑、预览、放在小编工作台   公告通知里面，公告记录，就变成只要记录发没发， 这样，思考，然后系统不允许两个地方设置，违反我的唯一性原理。小程序我担心和我们的数据库以及和后端配不上，需要检查，虽然只有几个页面也不多，但是pipeline细节，比如公告发布有没有发布，显示对不对，岗位管理的详细每个地方对不对的上，材料前端提交的是不是真的会落座到我们这边和后台     小程序我担心和我们的数据库以及和后端配不上，需要检查，虽然只有几个页面也不多，但是pipeline细节，比如公告发布有没有发布，显示对不对，岗位管理的详细每个地方对不对的上，材料前端提交的是不是真的会落座到我们这边和后台，要找小agent帮忙检查是否我i们还有再用mock的数据没有走neon，这个也需要检查一下，小程序，我们在后台修改的东西会正确对应上去吗，因为我们的页面内容很细，所以不能马虎。此处需要核实：  我的系统是以Dday为计算点，D+是该日期往后多少  D-是该日期往前算几天，就是提早的意思。
---

## 一、审计结论摘要

本次审计共发现 **11 类数据问题**，其中：

| 严重度 | 数量 | 说明 |
|--------|------|------|
| 🔴 P0 严重 | 3 项 | 前端数据脱节、选举状态不自洽、账号角色异常 |
| 🟠 P1 高 | 4 项 | 组织占位、候选人关联错误、选举结果缺失、选民数据不符 |
| 🟡 P2 中 | 4 项 | 公告格式、材料关联、提案关联、阶段日期 |

**核心风险**：前端页面展示的数据与后端数据库完全不一致，系统当前处于"前端 mock 数据 + 后端真实数据"两张皮状态，无法用于正式换届选举工作。

---

## 二、P0 严重问题

### P0-1 前端与后端数据完全脱节（架构级问题）

**现象**：前端页面展示的数据与数据库中的真实数据完全不同。

**证据**：

| 对比项 | 前端页面展示 | 数据库实际 |
|--------|-------------|-----------|
| 提案列表 | "涧口社区第十届换届选举提案"（2026-09-01提交） | "第15届涧口社区居委会换届方案"（2026-06-20提交） |
| 活动列表 | 4条硬编码数据（第十届~第七届） | 2条真实记录（el-11延寿村、el-15涧口社区） |
| 选举届数 | 第十届/第九届/第八届/第七届 | 第15届/第11届 |

**根因**：前端打包代码（`index-DK9G2iHy.js`）中硬编码了 mock 数据：
```javascript
// 前端硬编码的提案数据
let Ns=[{id:90,name:"涧口社区第十届换届选举提案",org_type:"community",
  election_mode:"全民直选",dday:"2026-10-30",status:"preparing",...}]

// 前端硬编码的活动数据
let iy=[{id:1,name:"涧口社区第十届换届",election_date:"2026-10-30",...},
        {id:2,name:"涧口社区第九届换届",election_date:"2026-07-15",...},
        {id:3,name:"涧口社区第八届换届",election_date:"2026-05-20",...},
        {id:4,name:"涧口社区第七届换届",election_date:"2026-03-10",...}]
```

**影响**：
- 张主任在前端看到的所有数据都是假的，无法据此开展工作
- 后端 API（`/api/elections`、`/api/candidates` 等）已实现但前端未对接
- 系统无法通过验收

**建议**：立即将前端各模块的数据获取逻辑从本地 mock 切换为后端 API 调用。

---

### P0-2 选举状态与日期严重不自洽

**现象**：两场选举的状态标记与实际日期严重矛盾。

#### el-15 涧口社区（第15届）
| 字段 | 值 | 问题 |
|------|-----|------|
| el_election_date | 2026-07-30 | 选举日 |
| el_status | in_progress（进行中） | 今天2026-09-01，已过去33天，不应仍为"进行中" |
| el_remark | 空 | 无任何说明 |

#### el-11 延寿村（第11届）
| 字段 | 值 | 问题 |
|------|-----|------|
| el_election_date | 2026-07-16 | 选举日 |
| el_status | in_progress（进行中） | 已过去47天 |
| el_remark | "D+5收尾中：备案完成、交接未开始" | 实际已过D+47，备注信息滞后42天 |
| D0阶段日期 | 2026-07-15 | 与选举日2026-07-16不一致，差1天 |
| election_results.er_election_date | 2026-07-15 | 与elections表的2026-07-16不一致 |

**影响**：选举状态是系统驱动流程流转的核心依据，状态错误会导致后续阶段无法推进或错误推进。

**建议**：
1. 将 el-15 状态更新为 finished（已完成）或根据实际情况更新
2. 将 el-11 状态更新为 finished，备注更新为实际进度
3. 统一 el-11 的选举日期为 2026-07-16（以 elections 表为准）

---

### P0-3 账号角色数据异常

**现象**：12个账号中11个的 roles 字段值不是合法角色标识。

| 账号 | roles 字段值 | 问题 |
|------|-------------|------|
| 张主任 | platform_admin | ✅ 唯一合法值 |
| 林秘书 | NO.001 | ❌ 不是角色标识 |
| 王组织 | NO.002 | ❌ |
| 陈审核 | NO.003 | ❌ |
| 刘备案 | NO.004 | ❌ |
| 赵归档 | NO.005 | ❌ |
| 孙监督 | NO.006 | ❌ |
| 周委员 | NO.001 | ❌ |
| 吴干事 | NO.002 | ❌ |
| 郑文书 | NO.003 | ❌ |
| 系统超管 | NO.001 | ❌ 且手机号脱敏为"130****0000"，其他账号未脱敏 |

**影响**：角色权限系统无法正常工作，11个账号可能无法登录或权限错误。

**建议**：将 roles 字段更新为合法角色标识（如 org_admin、reviewer、filer 等），统一手机号脱敏策略。

---

## 三、P1 高严重度问题

### P1-1 组织数据大量占位且信息不全

**现象**：
- 130+ 组织中大量名称为"XX镇社区占位1"、"XX镇社区占位2"等占位数据
- 所有组织的 orgPhone（联系电话）和 orgPerson（联系人）字段均为 null

**影响**：组织信息不完整，无法用于实际选举工作中的联系和通知。

**建议**：补全真实组织信息，清除占位数据。

---

### P1-2 候选人字段存储错误 + 状态滞后

**现象**：

1. **candPositionId 存储中文而非ID**：
   - 实际值："主任"、"副主任"、"委员"
   - 应为：positions 表中的 position_id（如 pos-001）

2. **el-15 候选人状态滞后**：
   - 7名候选人全部停留在"待第3轮区级联审"（r3_pending）
   - 但选举日2026-07-30已过33天，候选人审核应早已完成

3. **el-15 黄志明有票数但无结果记录**：
   - candidates 表中黄志明 cand_votes=28
   - 但 election_results 表中没有 el-15 的任何记录

**建议**：
1. 将 candPositionId 改为关联 positions 表的外键
2. 更新 el-15 候选人的实际审核状态和得票情况
3. 补全 el-15 的选举结果记录

---

### P1-3 选举结果数据不完整

**现象**：

| 选举ID | 结果记录数 | 问题 |
|--------|-----------|------|
| el-14（涧口2021年） | 4条 | er_result_ann_code 全部为 null |
| el-11（延寿村） | 6条 | er_handover_status 全部为"未交接"，选举已过47天 |
| el-15（涧口2026年） | 0条 | 选举已结束但无结果记录 |

**el-11 结果数据自洽性问题**：
- er_eligible_voters=1286（应有选民数）
- 但 voters 表中 el-11 只有 23 名选民记录
- 差距达 1263 人，数据严重不符

**建议**：
1. 补全 el-15 的选举结果
2. 补全 el-14 的结果公告编号
3. 推进 el-11 的交接工作或更新状态
4. 核实并补全 el-11 的选民名册

---

### P1-4 选民数据量与实际严重不符

**现象**：

| 选举ID | voters表记录数 | election_results中应有选民数 | 差距 |
|--------|---------------|---------------------------|------|
| el-11 | 23 | 1286 | -1263 |
| el-15 | 15 | 无结果记录 | 无法核对 |

**影响**：选民名册是选举合法性的基础，23名选民与1286名应有选民的差距意味着选民登记模块数据严重缺失。

**建议**：补全真实选民名册数据。

---

## 四、P2 中严重度问题

### P2-1 公告编号格式不统一

**现象**：announcements 表中 ann_code 字段格式混乱：
- 规范格式："第1号"、"第2号"、"第2-1号"
- 不规范格式："预选结果"、"预选办法"、"补充公告"（无编号）

**建议**：统一公告编号格式，所有公告应有唯一编号。

---

### P2-2 材料字段存储错误

**现象**：
- `matCandidateId` 存储候选人姓名（如"林强"），而非候选人ID
- `matFiles` 全部为空数组，无实际文件记录

**建议**：将 matCandidateId 改为 candidates 表外键，补全材料文件记录。

---

### P2-3 提案关联缺失

**现象**：
1. `elections.el_proposal_id` 全部为 null，但 proposals 表有4条记录
2. `proposals.prop_creator_id` 和 `prop_reviewer_id` 存储手机号（如"13800000002"），而非用户ID
3. el-15 的提案"第15届选举委员会成立方案"状态为 submitted（已提交），提交时间2026-06-22，但选举已进行到候选人阶段，该提案仍未审核

**建议**：建立选举与提案的关联，创建人/审核人使用用户ID，及时处理待审核提案。

---

### P2-4 阶段日期时区问题 + 阶段缺失

**现象**：
1. election_stages 表中日期使用 UTC 时间（如 `2026-07-15T16:00:00.000Z`），转换为北京时间是次日0点，可能导致前端显示日期偏移1天
2. el-11 缺少 D+1~D+10 的"结果备案、新旧班子交接"阶段记录

**建议**：统一使用北京时间存储日期，补全 el-11 的后续阶段。

---

## 五、数据统计总览

| 表名 | 记录数 | 主要问题 |
|------|--------|---------|
| accounts | 12 | 11个角色字段异常 |
| orgs | 130+ | 大量占位数据，联系信息全空 |
| elections | 2 | 状态与日期不自洽 |
| election_stages | 40 | 时区问题，el-11缺后续阶段 |
| positions | 4 | 基本正常 |
| candidates | 17 | 职位字段存中文，状态滞后 |
| announcements | 16 | 编号格式不统一，公示截止日全空 |
| materials | 6 | 候选人ID存姓名，文件全空 |
| voters | 38 | 数量与实际严重不符 |
| election_results | 10 | el-15无记录，el-11交接未完成 |
| proposals | 4 | 与选举无关联，创建人存手机号 |
| notifications | 0 | 无通知记录 |
| operation_logs | 0 | 无操作日志 |

---

## 只有1下午时间 全部做完
-
真实页面级字段校验完成。结果：

**核心字段 100% 可用**：
- elections 全部字段 100%（el_term 91%, el_note 36% 属合理）
- election_stages 100%（es_note 全空 —— 后端 SELECT 没返回 stageDescription，我读的是 `x.stageDescription || x.stDescription`，而后端返回的是 stageKey/stageName/stageStatus/stageStartDate/stageEndDate/stageOrder，**没有 description**）
- candidates 100%（cand_votes 48% 因部分未投票为 null，cand_note 全空）
- positions 100%
- results 大部分 OK

**发现的真问题（全空字段）**：

1. **`es_note` 全空** —— 后端 stages 接口不返回描述字段。我 `mapStages` 读 `x.stageDescription || x.stDescription`，两者后端都没有。
   - 影响：`notice.js` 用 `es_note` 显示阶段说明 → 会显示空/兜底文案。
   - 修法：后端 SELECT 加 `s.stage_description AS "stageDescription"`，或小程序用 `es_stage_name` 兜底（已有兜底逻辑）。

2. **`ann_edit_time` 全空** —— 后端 announcements SELECT 无 ann_edit_time/ann_reviewer/ann_review_time。
   - 影响：`home.js` 用 `ann_publish_time || ann_edit_time`，publish 21% 有值，所以还有 79% 情况显示空日期。
   - 修法：后端补列。

3. **`er_actual_voters` / `er_result_ann_code` 全空** —— 后端 results SELECT 只有 erVotes/erTurnout 等，没有这些列。
   - 影响：`notice.js` 用 `er_result_ann_code` 反查公告 → 查不到 → 显示无关联公告；`er_actual_voters` 显示 0 人投票。

4. **`ros_note` 全空** —— 后端 roster SELECT 无 rosNote。
5. **`cand_note` 全空** —— 后端 candidates SELECT 无 candNote。

6. **materials/notifications 无数据** —— 因为登录的是平台账号（boss），不是选民，没有自己的材料/通知。这是**数据问题不是映射问题**，但也说明这两个路径**没被真实验证到**。

7. **announcements `ann_publish_time` 仅 21%** —— 79% 的公告没有发布时间（可能是草稿）。home 页按 publish_time 排序，未发布的会排在后面。这是数据状态，合理。

**关键结论**：
- 映射层（我改的部分）**全部正确**，核心字段 100%
- 全空字段都是**后端 SELECT 没返回该列**（后端待办），不是小程序映射错误
- materials/notifications 因测试账号是平台账号而无数据，**未被真实验证**

现在我要：
1. 清理临时文件
2. 停止后端进程
3. 如实报告

让我先清理。

注意：后端进程还在跑，需要停掉。还有 _boot.log, _boot.err, _e2e.js, _e2e2.js, _e2e_out.txt, _e2e2_out.txt 需要删除。

先删除文件，再停进程。

另外，我应该把后端缺列的情况补进 db_structure.md 吗？用户没要求，但这是重要发现。我先报告，不擅自加。

实际上，我应该把关键的"后端待补列"记录到 db_structure.md，这是有价值的真相。但用户催得紧，我先清理+报告，问是否要记录。

让我清理。
真实页面级校验完成。**映射层全对，但暴露 6 个后端缺列问题**。清理临时产物。
删除工具限制在工作区内，backend 在工作区外。用命令行删除。

先停后端进程，再删文件。
删除工具限工作区内，改用命令清理，并停掉后端进程。
我的临时文件都删了，只剩一个 `_probe_org.js` —— 这不是我创建的（可能是之前遗留的，或者是别人的）。我不应该删除不是我创建的文件。让我确认一下它是不是我创建的。

我没创建过 `_probe_org.js`。它是既有文件，我不动它。

等等，让我检查一下它的内容，看是否是我之前某次操作留下的。我没有创建过这个名字的文件。保留它。

现在验证工作区状态干净，并做最终检查。

让我确认 miniprogram/scripts 干净，以及所有改动文件语法正确。

然后如实报告。

最终报告要点：
1. 诚实：页面 UI 渲染没测（需要微信开发者工具，我无法运行）
2. 已测：真实后端 + 真实数据库 + 真实映射 + 页面依赖字段可用率
3. 结果：映射层 100% 正确，D 日校验通过
4. 发现 6 个后端缺列问题（不是我的映射错误）
5. materials/notifications 因账号无数据未验证到

让我做最后的完整性检查。
清理完成（`_probe_org.js` 非我创建，保留）。最后确认工作区干净且语法无误。
全部通过，工作区干净。

## 自测报告（如实）

**先回答你的问题：之前没测过，现在补完了。但页面 UI 渲染我依然没测——那需要微信开发者工具，我跑不了模拟器。**

### 已做的真实验证（启动后端 + 连真实 Neon 库）

**1. 端到端数据链路** ✅
```
登录(真实账号) → /api/health 200(连上Neon)
→ /api/orgs 123行 → /api/elections 11行 → stages 16行
→ announcements 160行 / positions 20行 / candidates 23行
→ roster 14行 / results 11行 / notifications 7行
```
全部 `code:0`，**无一 404**（证明我改的端点 `/api/mp/materials/mine`、`/api/notifications`、`el.electionId` 都对）。

**2. D 日口径（你最担心的）** ✅
```
真实数据：D=2027-03-15, D-35 原始 start=2027-02-08
映射 → offset=-35 → 反推回 2027-02-08 ✅ 完全一致
偏移全 0 的异常阶段：0/16
```

**3. 页面依赖字段可用率**（用真实映射后数据逐页校验）
| 页面 | 结果 |
|---|---|
| home | elections/stages 100%；announcements `ann_code/title/pin/type/status` 100% |
| method | elections/stages/positions **全部 100%** |
| candidate | candidates **全部 100%**（votes 48% 因未投票为 null，合理） |
| notice | announcements/stages 100%；results 9 项中 7 项 ≥91% |
| profile | candidates **全部 100%** |
| material | materials 无数据（测试账号是平台账号非选民） |

### 发现 6 个真问题（都是后端 SELECT 缺列，不是映射错）

| 字段 | 后端是否返回 | 影响 |
|---|---|---|
| `es_note` | ❌ stages 接口无描述列 | notice 页阶段说明为空 |
| `ann_edit_time` | ❌ announcements 无此列 | home 页 79% 公告无日期（publish 仅 21%） |
| `er_actual_voters` | ❌ results 无此列 | notice 页投票人数恒 0 |
| `er_result_ann_code` | ❌ results 无此列 | notice 页关联公告查不到 |
| `ros_note` | ❌ roster 无此列 | home 页简介为空 |
| `cand_note` | ❌ candidates 无此列 | 备注为空 |

**这些要后端在 SELECT 里加列**，小程序侧改不动（我已经按"如实缺省不造假"处理，不编造数据）。

### 没测到的（明确告知）
1. **页面 UI 渲染**——需微信开发者工具真机/模拟器
2. **materials / notifications 映射**——测试账号是平台账号（boss），无本人材料/通知，返回 0 行，**这两条路径未被真实数据验证**

要我把这 6 个后端缺列补进 `db_structure.md` 作为后端待办清单吗？或者你给个选民账号手机号，我把 materials/notifications 也真实验一遍？小程序的问题