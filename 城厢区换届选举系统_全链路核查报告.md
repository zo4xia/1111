# 城厢区村居换届选举系统 · 全链路核查报告

> 核查日期：2026-09-02
> 核查范围：backend（Node/Express + Neon PostgreSQL）、miniprogram（微信小程序选民端）、web（React PC 管理端/小编工作台）
> 核查性质：只读审计，未修改任何代码或数据库数据
> 数据库：Neon 共享库（仅 SELECT 验证）

---

## 摘要

| 维度 | 结论 | 严重度 |
|------|------|--------|
| A. mock vs Neon | 架构上已接通后端，但存在多处硬编码演示数据与静默回落，真机部署后大概率全部走 mock | **高** |
| B. 公告发布链路 | 后端落库字段齐全（含 D-001 补列），附件按公告独立存储；但小程序端完全不消费公告附件 | **高** |
| C. 岗位管理 | 核心数据同源一致；岗位附件后端可上传落盘，但小程序端不展示不下载 | **中** |
| D. 材料全链路 | **关键缺陷：小程序提交调用了错误的后端端点（staff 端点而非选民端点），字段名也不匹配，选民提交必然失败**；审核→回写→通知链路后端完整；归档页 PC 端用硬编码 mock | **高** |
| E. 后台改→前端同步 | 启动/登录时全量拉取，页面 onShow 读共享 DB；但缓存无 TTL，无后台轮询，后端不可达时静默用旧缓存 | **中** |
| F. 部署 | BASE_URL 硬编码 127.0.0.1，urlCheck 关闭，无生产域名配置，JWT_SECRET 硬编码默认值 | **高** |
| G. 修 vs 重写 | 小程序端 mock 纠缠深度高（5608 行本地快照 + 三级回退 + 双态分支 + 多处硬编码），**重写更划算**；PC 端数据干净，无需重写 | — |

---

## A. mock vs Neon：小程序页面数据源核查

### A.1 数据架构（已实现）

小程序采用**三级回退**架构：

```
HTTP 后端（token 在位时）→ 微信云开发 → 本地 db.js（2026-07-21 演示快照）
```

- `miniprogram/data/source.js:25-54` — `refreshRemote()`：先读本地缓存秒开，有 token 则调 `http.syncAll()`，失败回落云开发或本地
- `miniprogram/data/http.js:34-91` — `syncAll()`：并发拉取 9 个端点（/api/orgs, /api/elections, /api/announcements, /api/positions, /api/candidates, /api/mp/materials/mine, /api/notifications, /api/roster, /api/results + 各届 stages），经 `map.js` 翻译后 `Object.assign(dbm.DB, mapped)` 原位覆盖
- `miniprogram/data/map.js` — 唯一翻译层，声明式 SCHEMA 驱动，后端驼峰→小程序蛇形
- 全部 7 个页面（home/notice/method/material/candidate/profile/login）均通过 `require('../../data/db').DB` 或 `app.scoped()` 读取共享 DB 对象

**结论：登录 + 后端在线时，页面数据理论上全部来自后端 API（Neon）。**

### A.2 硬编码演示数据与缺陷

| # | 位置 | 问题 | 严重度 |
|---|------|------|--------|
| A-1 | `miniprogram/utils/api.js:8` | `BASE_URL = 'http://127.0.0.1:8080'` 硬编码。真机部署后所有请求打到手机本机 127.0.0.1 → 连接失败 → 静默回落本地 mock，用户无感知 | **高** |
| A-2 | `miniprogram/pages/login/login.js:22-23` | 演示预填充 `phone: '13800000002'`, `password: '123456'` 硬编码在 data 中 | 中 |
| A-3 | `miniprogram/pages/login/login.js:268-280` | `_localLogin()` 离线回退：明文密码比对 `password !== '123456' && password !== acc.acc_password_hint`，生产环境不应存在前端明文鉴权 | 高 |
| A-4 | `miniprogram/pages/candidate/candidate.js:64` | `const isDone = elId === 'el-11'` 硬编码选举 ID 判断是否"全流程完结样本"，换届即失效 | 中 |
| A-5 | `miniprogram/pages/home/home.js:95` | 近期选举预告区注释明确写"内容取本届次【预置/填充】的下一阶段 nextStageRaw（演示数据，仅撑版面，非实时接口）" | 低 |
| A-6 | `miniprogram/pages/login/login.js:283-292` | `_guestLogin()` 游客模式：无 acc 记录时构造游客身份直接进入，roleKey='voters' 只读 | 中 |
| A-7 | `miniprogram/data/source.js:40-52` | 微信云开发作为第二级回退，但 `cloud.applyCfg('')` envId 留空，实际是否生效存疑 | 低 |
| A-8 | 全部页面 | 无任何页面展示"当前数据来源"标识（在线/离线/缓存），用户无法区分真实数据与演示数据 | 中 |

### A.3 serverMode 双态生效条件

- `app.js:12` — `serverMode: false` 初始值
- `http.js:83` — `syncAll()` 成功后设 `app.globalData.serverMode = true`
- `app.js:105` — `serverLogin()` 成功后设 `serverMode = true`
- `app.js:55` — `restoreServerSession()` 失败时设 `serverMode = false`
- `app.js:89` — `logout()` 设 `serverMode = false`
- **仅 material.js:165 显式检查 `g.serverMode`** 决定走真实提交还是本地暂存；其余 6 个页面均隐式依赖 DB 被覆盖，不检查 serverMode

### A.4 结论

- **架构 OK**：三级回退设计合理，syncAll 覆盖全面，map.js 翻译层干净
- **风险高**：BASE_URL 硬编码 + 静默回落 + 无数据来源标识 = 真机部署后用户大概率在看 2026-07-21 的演示快照而不自知
- **修复建议**：(1) BASE_URL 改为可配置（环境变量/构建注入）；(2) 后端不可达时顶部 toast 提示"当前为离线演示数据"；(3) 移除登录页硬编码演示账号；(4) 移除 candidate.js el-11 硬编码，改为数据驱动判断

---

## B. 公告发布链路核查

### B.1 后端落库字段（含 D-001 补列）

`backend/api.js` 公告相关端点：

| 端点 | 方法 | 行号 | 功能 |
|------|------|------|------|
| `/api/announcements` | GET | 510-526 | 列表，含 annFiles 目录扫描 |
| `/api/announcements` | POST | 528-539 | 创建草稿 |
| `/api/announcements/:id` | PUT | 551-572 | 全量编辑（标题/正文/编号/落款/成文日期/材料开关/发布方式/定时/提醒） |
| `/api/announcements/:id/publish` | PUT | 541-548 | 发布 |
| `/api/announcements/:id/file` | POST | 586-591 | 附件上传（每份公告独立目录） |

**D-001 要求补列已实现**（`api.js:991-1000` 启动时幂等 ALTER TABLE）：

```sql
ann_sign                text        -- 落款
ann_sign_date           varchar(40) -- 成文日期
ann_open_material_submit boolean    -- 材料开关
ann_publish_mode        varchar(16) -- 发布方式（immediate/scheduled）
ann_publish_at          timestamptz -- 定时发布时间
ann_remind_hours        int         -- 提醒提前小时数
ann_remind_to           varchar(60) -- 提醒对象
```

PUT `/api/announcements/:id`（行 551-572）完整接收并持久化上述全部字段，COALESCE 语义保证部分更新不丢值。

### B.2 公告附件独立存储

- 存储路径：`uploads/announcements/<公告UUID>/`（`api.js:577-579`）
- 每份公告独立目录，切换公告下载链接随该公告变化（`api.js:524` `filesOf('announcements/' + a.id)`）
- 静态文件服务：`app.use('/api/files', express.static(UPLOAD_DIR))`（`api.js:844`）
- 实测 uploads 目录下存在 35+ 个公告 UUID 子目录

**结论：后端公告附件存储正确，每份公告独立，不存在"下载链接写死"问题。**

### B.3 小程序端公告展示（关键缺陷）

`miniprogram/pages/notice/notice.js`：

- **公告列表/详情/阶段卡片/往届结果**：数据全部来自 `DB.announcements`，syncAll 覆盖后即为后端真实数据 ✅
- **公告附件**：**完全不消费**。`map.js:46-55` announcements SCHEMA 中没有任何 `annFiles`/`attachments` 映射规则，后端返回的 `annFiles` 字段被静默丢弃
- `notice.js:285-292` `copyAnnContent()`：仅提供"复制全文"功能，无附件下载/预览入口
- `notice.js` 整个文件无任何 `annFiles`/`attachments`/`downloadFile`/`wx.downloadFile` 引用

**"一天发多个公告、前端切换公告下载链接是死的"根因**：不是后端存储问题，而是小程序端根本没有实现公告附件的获取与展示。PC 端上传的公告附件，小程序用户完全看不到。

### B.4 结论

- **后端 OK**：落库字段齐全（含 D-001），附件按公告独立存储，发布/编辑/上传端点完整
- **缺陷高**：小程序端不消费公告附件，PC 端上传的附件在小程序端不可见
- **修复建议**：(1) `map.js` announcements SCHEMA 增加 `ann_files` 映射（from: 'annFiles'）；(2) notice.js 公告详情增加附件列表，点击调用 `wx.downloadFile` + `wx.openDocument`；(3) 首页公告列表增加附件图标提示

---

## C. 岗位管理核查

### C.1 后端岗位数据

`backend/api.js`：

| 端点 | 方法 | 行号 | 功能 |
|------|------|------|------|
| `/api/positions` | GET | 924-937 | 列表，含 posFiles 目录扫描 |
| `/api/positions/:id/file` | POST | 938-943 | 岗位附件上传 |

- 岗位通过提案审批联动自动创建（`api.js:457-467`）：proposal_posts → positions（覆盖式重建）
- 岗位附件存储：`uploads/positions/<岗位UUID>/`（`api.js:916-918`）
- 示意附件：`lib/stubFiles.js:28-38` `positionStubText()` 生成纯文本占位件，目录为空时自动生成，有真实文件时跳过

### C.2 小程序端岗位展示

`miniprogram/pages/method/method.js`：

- 岗位清单（行 90-102）：`DB.positions.filter(pos_org_id === g.orgId && pos_election_id === selectedElectionId)`，展示 `pos_type`/`pos_quota`/`pos_desc`
- 选举方式（行 87）：`selectedElection.method` ← `el_method`
- 选举日程（行 105-123）：`DB.election_stages` ← 后端 stages
- **岗位附件**：**不展示不下载**。method.js 中 `openMaterials()`（行 215-282）弹出的"材料下载"是**硬编码的 4 份文本模板**（换届选举报名表/个人自荐表/组织推荐函/材料清单说明），通过 `buildFormText()` 动态拼接，并非后端 positions 表的真实附件

### C.3 结论

- **核心数据同源一致 OK**：岗位类型/职数/说明、选举方式、日程均来自后端，PC 端与小程序端一致
- **缺陷中**：岗位附件后端可上传落盘，但小程序端用硬编码模板替代，不展示真实附件
- **修复建议**：(1) `map.js` positions SCHEMA 增加 `pos_files` 映射；(2) method.js 材料下载弹窗改为读取 `pos_files`，无附件时回落模板；(3) 模板文本可保留为"无附件时的兜底提示"，但应明确标注"模板"

---

## D. 材料全链路核查

### D.1 小程序提交（关键缺陷）

`miniprogram/pages/material/material.js:165-207` 在线提交逻辑：

```javascript
api.post('/api/materials', {
  electionId: g.electionId,
  matType: '参选人材料',
  note: noteText,
  submitter: ...,
  submitterPhone: ...,
  attachments: []
})
```

**缺陷 1：调用了错误的端点。** 后端有两个材料创建端点：

| 端点 | 行号 | 权限 | 用途 |
|------|------|------|------|
| `POST /api/mp/materials` | 760-775 | auth()（普通用户） | 选民小程序提交，mat_type='个人自荐' |
| `POST /api/materials` | 778-812 | auth() + **requireStaff** | 后台内推（组织推荐），需经办/编辑/审核权限 |

小程序调用的是 `POST /api/materials`，该端点要求 `requireStaff`（`api.js:78` STAFF_ROLES = operator/sub_admin/editor/reviewer/platform_admin）。普通选民角色为 `user`/`voters`，**必然返回 403 Forbidden**。

**缺陷 2：字段名不匹配。** 即使绕过权限，`POST /api/materials` 期望 `{name, phone, positionId, note, elId}`（`api.js:779`），小程序发送的是 `{electionId, matType, submitter, submitterPhone}`，`name` 和 `phone` 为 undefined → 后端返回 400 "姓名和手机号必填"。

**正确端点应为 `POST /api/mp/materials`**，该端点从 token 解析用户信息，仅需 `{positionId, note}`。

**结论：当前代码下，选民在小程序端提交材料必然失败（403 或 400），随后 catch 分支提示"提交失败"，不会进入附件上传流程。**

### D.2 附件上传

- 端点：`POST /api/materials/:id/upload`（`api.js:896-899`，兼容旧端点）或 `POST /api/materials/:id/file`（`api.js:826-843`）
- 存储路径：`uploads/<材料UUID>/`（`api.js:818`），**非**用户要求的 `uploads/<村或社区>/<届>/<分类>/<用户名>-<竞选岗位>_<上传时间>/`
- 小程序调用：`material.js:186` `api.uploadFile('/api/materials/' + matId + '/upload', ...)` — 端点正确，但因 D.1 创建失败永远不会执行到这里
- 首个附件上传后自动将材料状态从 pending → submitted（`api.js:837-841`）
- 实测：uploads 目录下**材料 UUID 子目录数量为 0**（仅有 announcements/positions/proposals 三个子目录），印证材料提交链路未真实跑通

### D.3 材料审核 → 回写 → 通知

`backend/api.js:847-893` `PUT /api/materials/:id/review`：

- **通过（approved）**：
  - 更新材料状态、审核人、审核时间、审核意见
  - 自动入候选人池：同届同手机号已有候选人则关联，否则 INSERT candidates（cand_r1='待审', cand_status='待初审'）（`api.js:858-873`）
  - 回写 `materials.mat_candidate_id`（`api.js:874`）
  - 插入通知（notifications 表，notif_type='material_approved'）（`api.js:876-883`）
- **驳回（rejected）**：
  - 更新材料状态、审核意见
  - 插入通知（notif_type='material_rejected'）（`api.js:884-891`）

**结论：审核→回写→通知链路后端完整，逻辑正确。**

### D.4 小程序"我的提交记录"同步

`material.js:41-78` `refresh()`：

- 从 `s.materials`（共享 DB，syncAll 覆盖）过滤 `mat_applicant_id === phone`
- 展示状态、审核意见、附件数量
- 提交成功后调用 `http.syncAll()`（`material.js:200`）刷新
- 离线时本地草稿（`localMaterialDrafts`）与服务端记录合并展示（`material.js:65-67`）

**结论：记录同步机制设计正确，但因 D.1 提交失败，实际无法验证端到端。**

### D.5 归档（Archives）

- 后端：`GET /api/archives`（`api.js:906`，SIMPLE listFactory），查询 archives 表
- **PC 端归档页**：`web/src/pages/Election/Archives/index.tsx` 使用 `useArchiveFolders()` from `utils/electionStore.ts`
- `electionStore.ts:125-158` — **硬编码 4 个归档文件夹**（涧口社区第七/八/九/十届），全部为 mock 数据，文件无 url
- `electionStore.ts:4` 注释明确写"mock 演示，后续接真实文件服务/接口替换即可"
- **后端 archives 表无数据写入逻辑**（无 INSERT 端点），仅有 SELECT
- 小程序端无归档页面

**结论：归档功能未真实实现，PC 端为硬编码 mock，后端仅有空表查询。**

### D.6 结论

- **严重缺陷高**：小程序材料提交调用错误端点 + 字段不匹配，选民提交必然失败
- **审核链路 OK**：后端审核→入池→回写→通知完整
- **归档缺陷中**：PC 端硬编码 mock，后端无写入逻辑
- **存储路径不一致中**：材料附件存 `uploads/<UUID>/`，非用户要求的归档结构
- **修复建议**：(1) material.js 提交改为 `POST /api/mp/materials`，字段改为 `{positionId, note}`；(2) 归档功能实现后端写入 + PC 端接真实 API；(3) 附件存储路径按用户要求重构为 `uploads/<org>/<term>/<category>/<username>-<position>_<timestamp>/`

---

## E. 后台改→前端同步核查

### E.1 启动刷新逻辑

`miniprogram/app.js`：

| 时机 | 调用 | 行号 |
|------|------|------|
| onLaunch | `source.refreshRemote()` | 17 |
| 登录恢复 | `restoreServerSession()` → `http.syncAll()` | 47-57 |
| 服务端登录成功 | `serverLogin()` → `http.syncAll()` | 98-113 |
| 材料提交成功 | `http.syncAll()` | material.js:200 |

`source.refreshRemote()` 流程（`source.js:25-54`）：
1. 读 `electionRemoteCache` 本地缓存 → `Object.assign(db.DB, cache)` 秒开
2. 有 token → `http.syncAll()` 拉后端 → 成功 return true
3. 无 token 或失败 → 尝试微信云开发
4. 都失败 → 保持本地 db.js

### E.2 缓存策略

- 缓存键：`electionRemoteCache`（`http.js:8`, `source.js:7`）
- 写入：`http.js:78` `wx.setStorageSync(CACHE_KEY, mapped)` — syncAll 成功后全量写入
- 读取：`source.js:27` — 启动时先读缓存秒开
- **无 TTL、无过期时间、无版本号** — 缓存永久有效，除非被新的 syncAll 覆盖
- 兼容旧键：`electionJsonbinCache`（`source.js:8`）

### E.3 页面刷新机制

- 全部 7 个页面均有 `onShow() { this.refresh() }`
- `refresh()` 从共享 DB 对象（`app.scoped()` / `DB`）读取数据
- `http.js:14-21` `rerender()`：syncAll 完成后调用栈顶页面的 `refresh()`，保证停留在页面上也能看到新数据

### E.4 结论

- **机制 OK**：启动/登录全量拉取 + 页面 onShow 刷新 + syncAll 后主动 rerender，PC 端修改后小程序重新进入即可看到
- **风险中**：
  1. 缓存无 TTL：后端不可达时启动用旧缓存，无"数据可能过期"提示
  2. 无后台轮询/推送：小程序驻留期间 PC 端修改，用户需切页面或重启才能看到
  3. `electionRemoteCache` 与本地 db.js 合并时用 `Object.assign` 顶层覆盖，若后端某表返回空数组会正确覆盖，但部分字段缺失时可能残留旧值
- **修复建议**：(1) 缓存增加 `cachedAt` 时间戳，超过 N 小时显示"数据可能已过期"；(2) 页面 onShow 时可考虑轻量增量刷新（如仅拉 announcements/materials）；(3) 后端可考虑 WebSocket 或轮询通知机制

---

## F. 部署配置核查

### F.1 小程序端

| 配置项 | 当前值 | 位置 | 问题 |
|--------|--------|------|------|
| BASE_URL | `http://127.0.0.1:8080` | `utils/api.js:8` | 硬编码 localhost，真机必失败 |
| urlCheck | `false` | `project.config.json:25` | 开发工具不校验合法域名，生产必须开启 |
| appid | `wx24c9aebda2b759a9` | `project.config.json:52` | 需确认是否为正式 appid |
| 生产域名 | 无 | — | 代码中无任何生产域名配置项 |
| HTTPS | 无 | — | BASE_URL 为 http，生产必须 https |

`api.js:6-7` 注释提到"生产环境将 BASE_URL 换成 https 域名并在小程序管理后台配置 request 合法域名"，但**未实现可配置化**，需手动改代码。

### F.2 PC 管理端

| 配置项 | 当前值 | 位置 |
|--------|--------|------|
| API baseURL | `''`（空，走相对路径） | `services/electionApi.ts:13` |
| vite dev proxy | `/api` → `http://localhost:8080` | `vite.config.js:69-76` |
| vite preview proxy | `/api` → `http://localhost:8080` | `vite.config.js:59-64` |
| 生产构建 | baseURL 仍为空，需 Nginx 反代 `/api` → 后端 | `configs/host.ts:13-16` |

PC 端部署相对干净，相对路径 + Nginx 反代即可，但需配置生产后端地址。

### F.3 后端

| 配置项 | 当前值 | 位置 | 问题 |
|--------|--------|------|------|
| JWT_SECRET | `'cxq-secret-dev-CHANGE-IN-PROD'` | `api.js:19` | 硬编码默认密钥，生产必须改 |
| JWT 过期 | `7d` | `api.js:63` | 注释说 12h，实际 7d |
| 端口 | `8080` | `api.js:18` | — |
| CORS | `*`（全开放） | `api.js:48` | 生产应收紧 |
| 数据库 | neon.env 文件 | `api.js:27-31` | 连接串明文存文件 |

### F.4 结论

- **严重度高**：BASE_URL 硬编码 + urlCheck 关闭 + JWT_SECRET 默认值 + CORS 全开放，生产部署需系统性整改
- **修复建议**：(1) 小程序 BASE_URL 改为构建时注入（如 `const BASE_URL = process.env.NODE_ENV === 'production' ? 'https://api.xxx.com' : 'http://127.0.0.1:8080'`）；(2) 生产环境开启 urlCheck 并配置合法域名；(3) JWT_SECRET 改为环境变量；(4) CORS 收紧为指定域名；(5) neon.env 加入 .gitignore

---

## G. 修 vs 重写评估

### G.1 小程序端 mock 纠缠深度量化

| 维度 | 数量/程度 | 证据 |
|------|-----------|------|
| 本地快照 db.js | **5608 行**，含 15+ 张表的完整演示数据 | `data/db.js` |
| 三级回退链路 | HTTP 后端 → 微信云开发 → 本地 db.js | `source.js:25-54` |
| serverMode 双态分支 | 仅 material.js 显式分支（1 处），其余 6 页隐式依赖 | `material.js:165` |
| 硬编码演示数据 | 登录页账号密码（1 处）、candidate el-11（1 处）、method 材料模板（4 份硬编码）、home 预告注释为演示数据、electionStore 归档 4 个文件夹 | 见 A.2、C.2、D.5 |
| 本地暂存/草稿 | material localMaterialDrafts、profile myUploads、login electionProfile、notice readNotices | 多处 |
| 缓存无 TTL | electionRemoteCache 永久有效 | `http.js:78` |
| 离线登录路径 | _localLogin（明文密码比对）+ _guestLogin（游客直接进入） | `login.js:268-292` |
| 云开发残留 | cloud.js + cloud.applyCfg('') + source.js 第二级回退 | `source.js:6,40-52` |
| 关键功能缺陷 | 材料提交端点错误（必然失败）、公告附件不消费、岗位附件不消费、归档 mock | 见 B/C/D |

**纠缠深度评分：高（7/10）。** 数据层架构本身干净（syncAll + map.js + 共享 DB），但外围的回退/缓存/硬编码/离线路径层层叠加，且存在 3 处关键功能缺陷。

### G.2 修 vs 重写结论

**结论：重写更划算，但建议先修关键缺陷救急。**

理由：
1. **数据层可复用**：`http.js` syncAll + `map.js` SCHEMA + `utils/api.js` HTTP 客户端设计良好，重写时可直接复用
2. **页面渲染逻辑可参考**：7 个页面的 WXML/WXSS/交互逻辑成熟，重写时可移植
3. **但需彻底清理的东西多**：5608 行 db.js、三级回退、离线登录、本地草稿、硬编码数据、无 TTL 缓存——这些在"干净端"中应全部删除
4. **关键缺陷修复成本不低**：材料提交端点错误需改 material.js + 验证全链路；公告/岗位附件需改 map.js + 3 个页面；归档需后端 + PC 端双写
5. **重写预估**：7 个页面 + 数据层，按"单通道 HTTP + 登录后可用 + 离线只提示不造数"原则，约 2-3 周可完成干净版

**分阶段建议**：
- **第一阶段（救急，1-2 天）**：修 material.js 提交端点、BASE_URL 可配置、移除登录页硬编码账号
- **第二阶段（补功能，1 周）**：公告附件消费、岗位附件消费、归档真实化
- **第三阶段（重写干净端，2-3 周）**：若第二阶段后仍觉维护成本高，启动重写，复用现有数据层和页面渲染

### G.3 若重写：后端接口契约清单

从 `backend/api.js` 提取的完整端点清单（干净端应依赖这些）：

#### 认证

| 端点 | 方法 | 入参 | 返回 | 说明 |
|------|------|------|------|------|
| `/api/login` | POST | `{phone, password, orgId}` | `{token, user}` | 密码登录（与 PC 端同源） |
| `/api/mp/register` | POST | `{phone, orgId, name?}` | `{token, user, orgName}` | 选民免密注册 |
| `/api/mp/login` | POST | `{phone, orgId, wxCode?}` | `{token, user}` | 选民免密登录 |
| `/api/mp/wxlogin` | POST | `{wxCode}` | 501（未配置） | 微信一键登录（暂不可用） |
| `/api/health` | GET | — | `{db, today}` | 健康检查 + 服务端今日 |

#### 组织与届次

| 端点 | 方法 | 入参 | 返回 | 说明 |
|------|------|------|------|------|
| `/api/orgs` | GET | — | `Org[]` | 公开，登录页下拉用 |
| `/api/elections` | GET | — | `Election[]` | 归属地隔离 |
| `/api/elections/:id/stages` | GET | — | `Stage[]` | 某届日程阶段 |

#### 公告

| 端点 | 方法 | 入参 | 返回 | 说明 |
|------|------|------|------|------|
| `/api/announcements` | GET | `?electionId=` | `Announcement[]`（含 annFiles） | 列表 |
| `/api/announcements` | POST | `{elId, annCode?, annTitle, annStageKey?, annContent?}` | 草稿 | 创建（PC 端用） |
| `/api/announcements/:id` | PUT | `{annTitle?, annContent?, annCode?, annSign?, annSignDate?, annOpenMaterialSubmit?, annPublishMode?, annPublishAt?, annRemindHours?, annRemindTo?}` | 更新 | 全量编辑 |
| `/api/announcements/:id/publish` | PUT | — | 发布 | 发布 |
| `/api/announcements/:id/file` | POST | multipart file | `{id, file, size}` | 附件上传 |

#### 岗位

| 端点 | 方法 | 入参 | 返回 | 说明 |
|------|------|------|------|------|
| `/api/positions` | GET | `?electionId=` | `Position[]`（含 posFiles） | 列表 |
| `/api/positions/:id/file` | POST | multipart file | `{id, file, size}` | 附件上传 |

#### 候选人

| 端点 | 方法 | 入参 | 返回 | 说明 |
|------|------|------|------|------|
| `/api/candidates` | GET | — | `Candidate[]`（含四轮审核字段） | 列表 |
| `/api/candidates/:id/round` | PUT | `{round:1-4, result:'通过'\|'不通过', reason?}` | 更新 | 人工录入轮次结果（PC 端用） |
| `/api/candidates/:id/result` | PUT | `{votes, status?}` | 更新 | 投票结果（PC 端用） |

#### 材料（选民端核心）

| 端点 | 方法 | 入参 | 返回 | 说明 |
|------|------|------|------|------|
| `/api/mp/materials/mine` | GET | — | `Material[]`（含 matFiles） | **我的材料列表（选民端用这个）** |
| `/api/mp/materials` | POST | `{positionId?, note?}` | `{id, matStatus, ...}` | **选民提交材料（选民端用这个！）** |
| `/api/materials/:id/upload` | POST | multipart file | `{id, file, size, total}` | 附件上传（兼容端点） |
| `/api/materials/:id/file` | POST | multipart file | `{id, file, size, total}` | 附件上传（主端点） |
| `/api/materials/:id/review` | PUT | `{status:'approved'\|'rejected', comment?}` | 更新 | 审核（PC 端用） |

#### 其他

| 端点 | 方法 | 说明 |
|------|------|------|
| `/api/notifications` | GET | 通知列表 |
| `/api/roster` | GET | 花名册 |
| `/api/results` | GET | 选举结果 |
| `/api/archives` | GET | 归档（当前空表） |
| `/api/dashboard/alerts` | GET | 仪表盘预警（PC 端用） |

**关键字段约定**：
- 所有 GET 返回驼峰字段（`AS "camelCase"`），如 `elId`, `annCode`, `matStatus`
- 统一响应包络：`{code: 0, data}`，code !== 0 为错误
- 认证：`Authorization: Bearer <token>`，JWT 7d 过期
- 材料状态值：`pending`（待审核）→ `submitted`（已提交，首附件后自动）→ `approved`/`rejected`
- 公告状态值：`draft` → `published`

### G.4 干净端推荐数据层设计

```
原则：单通道 = 仅 HTTP 真后端，删掉本地 mock 兜底；登录后才可用；离线只提示不造数

1. app.js onLaunch:
   - 检查 token → 无 token 直接跳登录页（不进首页看演示数据）
   - 有 token → http.syncAll() 全量拉取 → 成功进入首页；失败 toast "网络不可用，请检查网络后重试" + 重试按钮
   - 删除 source.js / cloud.js / db.js 整个文件

2. 数据存储:
   - 用全局 Map 或简单状态管理替代 db.js
   - syncAll 成功后写入全局状态
   - 页面 onShow 从全局状态读
   - 删除 electionRemoteCache 永久缓存（或改为 5 分钟短缓存 + 明确过期提示）

3. 页面:
   - 删除所有 serverMode 分支（只有在线一种模式）
   - 删除 localMaterialDrafts / myUploads 等本地暂存
   - 删除 _localLogin / _guestLogin 离线登录路径
   - 登录页移除硬编码演示账号

4. 离线处理:
   - 网络请求失败 → 页面顶部显示 "网络连接失败" banner + 重试按钮
   - 不造数、不回落、不静默降级
   - 已加载的数据可保留查看，但标注 "数据加载于 HH:mm，可能非最新"

5. 附件:
   - 公告附件：map.js 增加 ann_files 映射，notice 页增加附件列表 + wx.downloadFile/wx.openDocument
   - 岗位附件：map.js 增加 pos_files 映射，method 页材料下载改为读真实附件
   - 材料附件：提交后记录页显示附件列表，可点击预览
```

### G.5 PC 管理端 mockjs 残留核查

**结论：PC 端数据全部来自 /api，mockjs 仅为模板残留依赖，不影响页面数据。**

证据：
- `web/package.json` devDependencies 含 `mockjs@^1.1.0`、`@types/mockjs@^1.0.6`、`vite-plugin-mock@^3.0.2`
- `vite.config.js:45-49` — mock 插件仅在 `mode === 'mock'` 时启用（`npm run dev:mock`），正常开发/构建不启用
- `web/mock` 目录**不存在**（vite-plugin-mock 配置的 mockPath 无对应目录）
- `web/src` 全量搜索 `mockjs|Mock`：仅 `pages/Detail/Advanced/consts.ts` 中有 3 行注释 `// Mock Data of ...`，该文件为模板遗留的产品详情页常量，与选举业务无关
- `services/electionApi.ts` — 唯一 API 调用层，全部走 `axios.create({baseURL: ''})` → `/api/*`，无任何 mock 拦截
- `configs/host.ts` — 所有环境（development/test/release/site）的 API 均为 `''`（相对路径走 vite proxy/Nginx 反代），仅 mock 环境有独立配置但 API 也为空
- 选举业务页面（Election/* 下 12 个页面）均通过 `electionApi.ts` 调用后端，无硬编码业务数据

**唯一例外**：`utils/electionStore.ts` 中的归档文件夹（4 个硬编码 mock）和选举活动预置数据（1 条），但该文件有 `setLiveActivities()` 方法供 `liveSync.ts` 用后端真实数据替换。归档部分未接后端。

---

## 附录：核查方法说明

1. **代码静态分析**：全量阅读 backend/api.js（1012 行）、miniprogram 全部 7 个页面 JS + 数据层（source/http/map/db/api/kit）、web 核心服务层与配置
2. **文件系统核查**：uploads 目录结构实测（announcements 35+ 目录、positions 3+ 目录、proposals 目录、材料 UUID 目录 0 个）
3. **数据库只读验证**：Neon 共享库仅 SELECT，未执行任何写操作
4. **依赖核查**：web package.json mockjs 依赖、vite.config.js mock 插件启用条件
5. **未执行**：未启动后端服务做端到端 API 测试（避免对共享库产生任何写操作风险）；未在微信开发者工具中运行小程序

---

*报告完*
